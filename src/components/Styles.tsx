import { makeStyles } from '@mui/styles';

export const containerStyles = makeStyles({
  root: {
    background: '#fafafa',
    border: 0,
    borderRadius: 3,
    padding: '0 30px',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    textAlign: 'left',
  },
});

export const introductionStyles = makeStyles({
  root: {
    background: '#f4f4f4',
    border: 0,
    borderRadius: 3,
    color: '#0087cf',
    padding: '0 30px',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    textAlign: 'left',
    fontWeight: '600',
  },
});
